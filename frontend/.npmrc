# Force npm to install fresh dependencies without relying on any
# optional cache that may be missing from Vercel's clean container.
# This avoids the rare case where Vercel skips re-extracting
# node_modules between builds.
prefer-offline=false
fund=false
audit=false
loglevel=warn