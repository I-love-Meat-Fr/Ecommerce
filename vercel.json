{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "outputDirectory": "public",
  "buildCommand": "echo 'Skipping build — using prebuilt public/ from repo' && exit 0",
  "installCommand": "echo 'No install needed — using prebuilt public/' && exit 0",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}